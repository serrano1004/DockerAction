# .github/workflows/main.yml
name: CI Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  ci_pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: InfraConfig
        uses: ./actions/dockeraction # Usa la action creada en este repositorio.
        with:
          command: "echo Custom Command"

      - name: Build
        run: echo "Building"

      - name: Deploy
        run: echo "Deploying"

      - name: Test
        run: echo "Test"
